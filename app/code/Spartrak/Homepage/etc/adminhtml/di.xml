<?xml version="1.0"?>
<!--
  The banner image uploader.

  It is Spartrak\Homepage\Model\Image\Uploader — this module's own class, NOT a
  virtual type of Magento\Catalog\Model\ImageUploader. That class carries
  Magento_MediaGalleryCatalogIntegration's `save_category_image` plugin, which
  force-feeds every saved file to the media-gallery synchroniser and broke every
  banner save with "Could not import media assets for files:
  spartrak/homepage/<file>.webp". Disabling that plugin on a virtual type does
  not work — the full finding, including why, is written up on the class itself.

  Only the two paths' OWNER differs from before: they are no longer declared
  here at all. The uploader reads them from Model\Image\Storage's constants,
  which is the one place this module states where banner artwork lives, so the
  write side and the storefront's read side cannot drift apart.

  ALLOWED TYPES: raster formats plus WebP. Both lists stay here because they are
  policy a developer may want to change without touching PHP, and because the
  extension list and the MIME list have to be kept in step with each other.

  SVG is deliberately NOT allowed. An SVG is an executable document — a banner is
  admin-uploaded, but an admin account is exactly what an attacker escalates to,
  and a stored SVG served from the media domain is a stored-XSS primitive. WebP
  covers the "small file" case that would otherwise tempt someone to reach for
  SVG.
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="urn:magento:framework:ObjectManager/etc/config.xsd">

    <type name="Spartrak\Homepage\Model\Image\Uploader">
        <arguments>
            <argument name="allowedExtensions" xsi:type="array">
                <item name="jpg" xsi:type="string">jpg</item>
                <item name="jpeg" xsi:type="string">jpeg</item>
                <item name="png" xsi:type="string">png</item>
                <item name="gif" xsi:type="string">gif</item>
                <item name="webp" xsi:type="string">webp</item>
            </argument>
            <argument name="allowedMimeTypes" xsi:type="array">
                <item name="jpg" xsi:type="string">image/jpg</item>
                <item name="jpeg" xsi:type="string">image/jpeg</item>
                <item name="png" xsi:type="string">image/png</item>
                <item name="gif" xsi:type="string">image/gif</item>
                <item name="webp" xsi:type="string">image/webp</item>
            </argument>
        </arguments>
    </type>

</config>
